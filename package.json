{
  "name": "moneki-fullstack",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "dev": "cd backend && npm run dev",
    "build": "cd frontend && npm install && npm run build && cd .. && rm -rf public && cp -r frontend/dist public",
    "start": "cd backend && npm start"
  },
  "dependencies": {
    "cors": "^2.8.5",
    "csv-parser": "^3.0.0",
    "dotenv": "^17.4.2",
    "express": "^4.18.2",
    "openai": "^7.5.0",
    "sql.js": "^1.10.3"
  }
}
